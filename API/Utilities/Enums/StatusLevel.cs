using System.Reflection.Metadata.Ecma335;

namespace API.Utilites.Enums
{
    public enum StatusLevel
    {
        Requested = 1,
        OnProgress = 2,
        Completed = 3, 
        Cancelled = 4
        
    }
}
